/*
// recursively merge the arrays passed in to produce a single array with the elements combined such that each
// array passed in overwrites the values of any duplicate keys in the previous array, or extends the array if new
// values with different values reside under the same keys

export default configurator;

let base = require('./base.json');
let overrides = require('./overrides.json');

class Configurator{

    assembleConfigs(jsonElements){
        for(elem of jsonElements){
            config = ...[jsonElements];
        }
    }
};
*/